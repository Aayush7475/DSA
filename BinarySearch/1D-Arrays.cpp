#include<bits/stdc++.h>
using namespace std ;

// Searching X in the array ->
vector<int> BS(vector<int> nums , int n , int target){
    
}
int main(){
    int n ;
    cout << "Enter the number of elements in the array : ";
    cin >> n ;

    vector<int> nums(n);
    cout << "Enter the elements in the array : ";
    for(int i = 0 ; i < n ; i++){
        cin >> nums[i]  ;
    }
    cout << endl ;

}